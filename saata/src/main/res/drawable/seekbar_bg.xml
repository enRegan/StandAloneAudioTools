<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!--<item android:id="@android:id/background">-->
    <!--<bitmap android:src="@drawable/seek_bg"/>-->
    <!--</item>-->
    <item android:id="@android:id/secondaryProgress">
        <!--<clip>-->
        <shape>
            <corners android:radius="20dp" />
            <gradient
                android:startColor="@color/progress_dialog_bg"
                android:centerColor="@color/progress_dialog_bg"
                android:endColor="@color/progress_dialog_bg" />
            <!--<solid android:color="@color/black"/>-->
            <!--<solid android:color="@color/colorAccent"/>-->
            <!--<gradient android:startColor="@color/seekbar_left"-->
            <!--android:endColor="@color/seekbar_right"/>-->
        </shape>
        <!--</clip>-->
    </item>
    <item android:id="@android:id/progress">
        <clip>
            <shape>
                <corners android:radius="50dp" />
                <!--<solid android:color="@color/black"/>-->
                <gradient
                    android:startColor="@color/seekbar_left"
                    android:endColor="@color/seekbar_right" />
            </shape>
        </clip>
    </item>
</layer-list>