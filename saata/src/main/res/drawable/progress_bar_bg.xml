<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <item android:id="@android:id/background">
        <shape>
            <corners android:radius="5dip" />
            <gradient
                android:startColor="@color/progress_dialog_bg"
                android:centerColor="@color/progress_dialog_bg"
                android:endColor="@color/progress_dialog_bg" />
        </shape>
    </item>

    <item android:id="@android:id/secondaryProgress">
        <clip>
            <shape>
                <corners android:radius="5dip" />
                <gradient
                    android:startColor="#80ffd300"
                    android:centerColor="#80ffb600"
                    android:endColor="#a0ffcb00" />
            </shape>
        </clip>
    </item>

    <item android:id="@android:id/progress">
        <clip>
            <!--<shape>-->
            <!--<corners android:radius="5dip" />-->
            <!--<gradient-->
            <!--android:startColor="@color/progress_left"-->
            <!--android:endColor="@color/progress_right"-->
            <!--/>-->

            <!--</shape>-->
            <bitmap android:src="@drawable/progress_bar" />
        </clip>
    </item>
</layer-list>